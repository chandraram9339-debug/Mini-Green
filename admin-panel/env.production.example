# Скопируйте в .env.production перед `pnpm build`, если API на другом origin.
# Пустой или отсутствующий VITE_API_BASE = запросы на тот же хост/порт, что и админка (удобно за reverse-proxy).
# Пример отдельного API:
# VITE_API_BASE=https://api.example.com
